from distutils.core import setup

setup(name='fdmb',
      version='1',
      description='Python bindings for the fdmb',
      author='Wolfgang Mader',
      author_email='Wolfgang.Mader@fdm.uni-freiburg.de',
      url='https://github.com/ReedWood/fdmb',
      package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}' },
      license='GPLv3',
      long_description='fdmb is a toolbox to estimate parameters in the linear
                        state space model. It is implemented as a shared C library.
                        This packages provides python bindings for the fdmb.',
      packages=['fdmb']
      )
